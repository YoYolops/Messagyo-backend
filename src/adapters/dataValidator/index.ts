import Joi from "joi";
import { DataValidator } from "./types";

export default function dataValidator(): DataValidator {
    function verify(schema: any) {
        schema
    }

    return {
        verify: () => 
    }
}